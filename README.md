# amazun

A fake store for scratching the shopping itch. Browse, cart, checkout, fake order confirmation. Nothing ships, nothing is collected, no payment, no login.

Plain HTML/CSS/JS - no build step. Products live in `products.js`.

## Deploy
Hosted free on Vercel: import this repo at vercel.com/new, auto-deploys on every push to main.
